# Prints de alunos — /pac

Coloque aqui os prints com os nomes listados em `src/data/pac.ts` (`pacPrints`).
Só aparece na página o arquivo que existir. Formato ideal: JPG vertical (print de celular).
Antes de subir, borre nome, foto e número de quem não autorizou.
